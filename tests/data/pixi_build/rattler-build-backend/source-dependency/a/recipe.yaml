package:
  name: a
  version: 0.1.0

build:
  script:
    interpreter: python
    content: |
      import os

      os.makedirs(os.environ['PREFIX'], exists_ok=True)

      with open(os.path.join(os.environ['PREFIX'], 'hi'), 'w') as f:
          f.write("hello from package a!\n")
