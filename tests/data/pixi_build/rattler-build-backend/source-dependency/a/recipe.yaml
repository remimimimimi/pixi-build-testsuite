package:
  name: a
  version: 0.1.0

build:
  script:
    - if: win
      then: |
        mkdir "%PREFIX%"
        echo echo hello from package a! > "%PREFIX%\hi"
      else: |
        mkdir $PREFIX
        echo "hello from package a!" > $PREFIX/hi
